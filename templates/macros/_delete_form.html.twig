<form method="post" action="{{ path('macros_delete', {'id': macro.id}) }}" id="macro-delete-form" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ macro.id) }}">
    <button class="macro-delete"><i class="fas fa-trash-alt"></i></button>
</form>
